# ============================================================================
# PULLBACK STRATEGY V4 - ADAPTED TO 5M TIMEFRAME
# ============================================================================
# 1m Result: V4 achieved -$7.45 expectancy (30.23% WR, W/L 1.72)
# 1m Problem: Too much noise, tight stops getting hit, hard to reach TPs
#
# 5M ADAPTATION - Cleaner signals, better Win Rate expected:
#   1. Wider SL: 2.25% → 3.0% (accommodate 5m candle range)
#   2. Higher TP: 7.0% → 10.5% (5m moves are bigger)
#   3. Same RSI: 50-65 (proven pullback zone)
#   4. Same Volume: 3.5x (proven filter)
#   5. Confirmation: 15m (one level up)
#
# Expected with 5m:
# - Trades: 10-20/month (vs 40-50 in 1m) - MORE SELECTIVE
# - Win Rate: 35-40% (vs 30% in 1m) - LESS NOISE
# - Avg Win: $120+ (vs $72 in 1m) - BIGGER MOVES
# - Avg Loss: $45-50 (vs $42 in 1m) - Similar
# - Expectancy: TARGET POSITIVE +$5-15 🎯
# ============================================================================

# Leverage Settings
DEFAULT_LEVERAGE=10
MAX_LEVERAGE=12
MIN_LEVERAGE=8

# Risk Management - REBALANCED (V3.5)
MAX_EQUITY_PER_TRADE=0.10  # 10% per trade (reduced from 15%)
MAX_LOSS_PER_TRADE=0.03    # 3% max loss per position
DAILY_DRAWDOWN_LIMIT=0.99  # Disabled for testing
GLOBAL_DRAWDOWN_LIMIT=0.99 # Disabled for testing
LOSS_STREAK_LIMIT=100      # Disabled for testing
COOLDOWN_HOURS=12

# Trading Parameters - 5M ADAPTED (Wider stops and TPs)
BASE_SL_PCT=0.030          # 3.0% stop loss (wider for 5m candles)
RISK_REWARD_RATIO=3.5      # 10.5% TP1 target (5m moves are bigger)
MAX_SPREAD=0.0025

# Volatility Thresholds
VOL_RATIO_MIN=0.6
VOL_RATIO_MAX=3.0
VOL_RATIO_HIGH_THRESHOLD=1.5
VOL_RATIO_LOW_THRESHOLD=0.9

# Volume Filters - BALANCED (Sweet spot between V1 and V2)
VOL_MULTIPLIER_20=3.5      # Balanced volume 3.5x (V1:2.5x, V2:4.0x, Ultra:5.5x)
VOL_MULTIPLIER_3=2.75      # Recent volume confirmation

# RSI Thresholds - PULLBACK ZONE (50-65 for LONG, 35-50 for SHORT)
RSI_LONG_THRESHOLD=50      # Enter LONG in pullback zone (NOT exhaustion >73)
RSI_LONG_MAX=65            # Don't enter if RSI > 65 (too hot)
RSI_SHORT_THRESHOLD=50     # Enter SHORT in pullback zone (NOT exhaustion <27)
RSI_SHORT_MIN=35           # Don't enter if RSI < 35 (too cold)
RSI_INVALIDATION_LONG=20   # Don't exit until RSI < 20 (not used in NONE mode)
RSI_INVALIDATION_SHORT=80  # Don't exit until RSI > 80 (not used in NONE mode)

# Invalidation Mode - NO EARLY EXITS
# NONE = No invalidation, only stops (best for 1m data with noise)
# LIGHT = Only RSI invalidation
# MODERATE = RSI + VWAP invalidation
# AGGRESSIVE = RSI + VWAP + EMA cross (too sensitive for 1m)
INVALIDATION_MODE=NONE

# Indicator Periods
SMA_SHORT_PERIOD=50
SMA_LONG_PERIOD=200
EMA_FAST_PERIOD=9
EMA_SLOW_PERIOD=20
RSI_PERIOD=14
ATR_PERIOD=14

# Timeframes - 5M PRIMARY
PRIMARY_TIMEFRAME=5m
CONFIRMATION_TIMEFRAME=15m

# Position Management - FINAL OPTIMIZATION (LET WINNERS RUN)
TP1_CLOSE_PCT=0.50         # Close 50% at TP1 (let more position run)
ENABLE_TRAILING_STOP=True
TRAILING_STOP_ACTIVATION=2.0  # Activate at 2.0R (was 1.8R - max patience)
TRAILING_STOP_DISTANCE=0.5    # Trail 0.5R behind

# ============================================================================
# EXPECTED RESULTS WITH 5M TIMEFRAME ADAPTATION:
#
# WHY 5M SHOULD WORK BETTER THAN 1M:
# ✅ Less noise → cleaner signals, better Win Rate
# ✅ Bigger moves → TPs more achievable ($120+ wins vs $72)
# ✅ Safer stops → 3% accommodates 5m candle range
# ✅ Better confluence → multi-timeframe alignment clearer
# ✅ Proven filters → RSI 50-65 and VOL 3.5x still apply
#
# Expected Performance (5m):
# - Trades: 10-20/month (0.3-0.7/day) - HIGHLY SELECTIVE
# - Win Rate: 35-40% (vs 30% in 1m) - LESS NOISE
# - Avg Duration: 6-12 hours (vs 3h in 1m) - LONGER HOLDS
# - Avg Loss: $48-52 (3% SL vs 2.25% in 1m)
# - Avg Win: $120-140 (10.5% TP vs 7% in 1m) - BIGGER MOVES
# - Win/Loss Ratio: >2.5 (vs 1.72 in 1m)
# - Profit Factor: >1.3 (TARGET!)
# - Expectancy: POSITIVE $10-20 🎯
#
# Mathematical Expectancy Targets (5m):
#
# Conservative (35% WR, W/L 2.5):
# E = (0.35 × $120) - (0.65 × $48) = $42 - $31.2 = +$10.80 ✅
#
# Base Case (37% WR, W/L 2.6):
# E = (0.37 × $130) - (0.63 × $50) = $48.1 - $31.5 = +$16.60 ✅✅
#
# Optimistic (40% WR, W/L 2.8):
# E = (0.40 × $140) - (0.60 × $50) = $56 - $30 = +$26 ✅✅✅
#
# Comparison 1m vs 5m:
# V4 1m BTC:  43 trades, 30.23% WR, $72 win, $42 loss, W/L 1.72, -$7.45 exp
# V4 5m:      TARGET 10-20 trades, 37% WR, $130 win, $50 loss, POSITIVE +$15 🚀
#
# This is THE solution. 5m eliminates 1m noise while keeping proven logic.
# ============================================================================
