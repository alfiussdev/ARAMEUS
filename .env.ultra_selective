# ============================================================================
# ULTRA SELECTIVE CONFIGURATION - FOR QUALITY OVER QUANTITY
# ============================================================================
# Based on backtest results showing massive over-trading (15 trades/day)
# Goal: Reduce to 1 trade/day (~30 trades/month) with 35%+ win rate
# ============================================================================

# Leverage Settings
DEFAULT_LEVERAGE=10
MAX_LEVERAGE=12
MIN_LEVERAGE=8

# Risk Management - REBALANCED (V3.5)
MAX_EQUITY_PER_TRADE=0.10  # 10% per trade (reduced from 15%)
MAX_LOSS_PER_TRADE=0.03    # 3% max loss per position
DAILY_DRAWDOWN_LIMIT=0.99  # Disabled for testing
GLOBAL_DRAWDOWN_LIMIT=0.99 # Disabled for testing
LOSS_STREAK_LIMIT=100      # Disabled for testing
COOLDOWN_HOURS=12

# Trading Parameters - TIGHTER STOPS, CLOSER TPS
BASE_SL_PCT=0.025          # 2.5% stop loss (tighter - was 5%)
RISK_REWARD_RATIO=2.0      # 5% TP1 target (more achievable - was 15%)
MAX_SPREAD=0.0025

# Volatility Thresholds
VOL_RATIO_MIN=0.6
VOL_RATIO_MAX=3.0
VOL_RATIO_HIGH_THRESHOLD=1.5
VOL_RATIO_LOW_THRESHOLD=0.9

# Volume Filters - V3.25 (Relaxed from V3.5 after 0 trades)
VOL_MULTIPLIER_20=5.5      # Volume must be 5.5x average (between V3 5x and V3.5 6x)
VOL_MULTIPLIER_3=4.25      # Recent volume must be 4.25x

# RSI Thresholds - V3.25 (Slightly relaxed)
RSI_LONG_THRESHOLD=73      # Only enter LONG with RSI > 73 (between V3 75 and V3.5 72)
RSI_SHORT_THRESHOLD=27     # Only enter SHORT with RSI < 27
RSI_INVALIDATION_LONG=20   # Don't exit until RSI < 20 (not used in NONE mode)
RSI_INVALIDATION_SHORT=80  # Don't exit until RSI > 80 (not used in NONE mode)

# Invalidation Mode - NO EARLY EXITS
# NONE = No invalidation, only stops (best for 1m data with noise)
# LIGHT = Only RSI invalidation
# MODERATE = RSI + VWAP invalidation
# AGGRESSIVE = RSI + VWAP + EMA cross (too sensitive for 1m)
INVALIDATION_MODE=NONE

# Indicator Periods
SMA_SHORT_PERIOD=50
SMA_LONG_PERIOD=200
EMA_FAST_PERIOD=9
EMA_SLOW_PERIOD=20
RSI_PERIOD=14
ATR_PERIOD=14

# Timeframes
PRIMARY_TIMEFRAME=1m
CONFIRMATION_TIMEFRAME=5m

# Position Management - OPTIMIZED FOR WIN RATE
TP1_CLOSE_PCT=0.60         # Close 60% at TP1 (lock profits faster)
ENABLE_TRAILING_STOP=True
TRAILING_STOP_ACTIVATION=1.2  # Activate at 1.2R (earlier protection)
TRAILING_STOP_DISTANCE=0.5    # Trail 0.5R behind

# ============================================================================
# EXPECTED RESULTS WITH V3.5 RISK MANAGEMENT REBALANCE:
# - Trades: ~50-60/month (2/day) - same as V3.25
# - Win Rate: 35-45% instead of 24% (closer TPs more achievable)
# - Avg Duration: 2-4 hours (same)
# - Avg Loss: ~$26 instead of $51 (tighter 2.5% SL)
# - Avg Win: ~$52 instead of $76 (closer 5% TP)
# - Profit Factor: >1.2 instead of 0.48 (better win/loss ratio)
# - Expectancy: POSITIVE instead of -$20.22
# ============================================================================
